<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ post.title }}</title>
</head>
<body>
<h1>{{ post.title }}</h1>
<p>Nombre del creador: {{ post.author }}</p>
<p>Fecha del post: {{ post.dateCreated|date('Y-m-d') }}</p>
<p>Cuerpo {{ post.body }}</p>
<img src="{{ profilePictureUrl }}" alt="Foto de perfil del creador">


</body>
</html>